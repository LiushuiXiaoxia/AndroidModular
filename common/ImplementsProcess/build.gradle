apply plugin: 'java'

sourceCompatibility = "8"
targetCompatibility = "8"

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // compile project(':JavaBase')
    implementation "cn.mycommons:JavaBase"
}